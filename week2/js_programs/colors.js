const colors = ['red', 'blue', 'green']
console.log(colors.map(function(item) {
	return '<li>' + item + '</li>';
}));

//console.log(colors)

const colorsList = colors.map(function(item) {
	return '<li>' + item + '</li>';
})
console.log(colorsList)
